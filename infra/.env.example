# Optional. If omitted, CDK creates Secrets Manager secrets with the default names.
# Set these when you already created the secrets manually and want CDK to reference them.
LINE_SECRET_NAME=receipt-line-agent/prod/line
GOOGLE_SECRET_NAME=receipt-line-agent/prod/google

# Optional model override.
BEDROCK_MODEL_ID=global.anthropic.claude-haiku-4-5-20251001-v1:0
