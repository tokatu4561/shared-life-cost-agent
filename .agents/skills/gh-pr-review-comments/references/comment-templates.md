# Comment Templates

## 互換性
- `X` が削除されているため、既存クライアントで失敗する可能性があります。互換期間の維持か、明示的な移行エラーを検討してください。

## 仕様整合
- 実装では `X` が追加/変更されています。`Y` の仕様定義も合わせないと、ドキュメントと実装がずれます。

## データ範囲
- 現在の `X` は候補全件を返すため、実際に処理した対象より広くなる可能性があります。実結果ベースで再構成するか、名称を明確化してください。

## テスト
- この変更で `X` の回帰観点が薄くなっているため、少なくとも `Y` のケースを残すと安全です。
